@model IEnumerable<Control.Web.Core.Models.InventarioTiposMovimientos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Positivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Positivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoMovimiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoMovimiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoMovimiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
